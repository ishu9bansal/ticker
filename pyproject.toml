[project]
name = "ticker"
version = "0.1.0"
description = "A simple FastAPI backend project for learning deployment"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.128.0",
    "kiteconnect>=5.0.1",
    "uvicorn>=0.40.0",
]
